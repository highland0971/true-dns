# Bundled Go toolchain (installed by the setup script in this repo clone)
.toolchain/

# Build output (root-anchored: cmd/truedns/ must stay tracked)
/dist/
*.exe
/truedns
/truedns.exe

# Local runtime artifacts
truedns-state.json
truedns.log
config.local.toml

# Editors / OS
.idea/
.vscode/
.DS_Store
